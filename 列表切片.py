#coding:utf-8    #转换utf－8，mac不支持自动转换

A=['charls','martina','michael','florence','eli']

print(A[0:3])      #0位到2位

print(A[1:4])      #1位到3位

print(A[:4])        #0位到3位，：前不填写，即从0位开始


print(A[1:])        #1位到最后一位，：后不填写，即输出至最后一位

print(A[-3:])        #负数索引返回离列表末尾相应距离的元素,返回倒数三个








